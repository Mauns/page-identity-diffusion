import StyleDictionary from "style-dictionary"
import { themer } from "../helper/themer.js"

export const tailwindColorsFormat = {
    name: "tailwindColorsFormat",
    formatter: (dictionary, options) => {
        
    }
}